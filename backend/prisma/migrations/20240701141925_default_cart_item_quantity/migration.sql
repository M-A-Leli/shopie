BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[CartItems] ADD CONSTRAINT [CartItems_quantity_df] DEFAULT 1 FOR [quantity];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
